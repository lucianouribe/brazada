<p id="notice"><%= notice %></p>

<h1>Api Misvis</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @api_misvis.each do |api_misvi| %>
      <tr>
        <td><%= link_to 'Show', api_misvi %></td>
        <td><%= link_to 'Edit', edit_api_misvi_path(api_misvi) %></td>
        <td><%= link_to 'Destroy', api_misvi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Api Misvi', new_api_misvi_path %>
