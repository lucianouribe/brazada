<%= form_for(api_tarifa) do |f| %>
  <% if api_tarifa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_tarifa.errors.count, "error") %> prohibited this api_tarifa from being saved:</h2>

      <ul>
      <% api_tarifa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
