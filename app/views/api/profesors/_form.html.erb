<%= form_for(api_profesor) do |f| %>
  <% if api_profesor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_profesor.errors.count, "error") %> prohibited this api_profesor from being saved:</h2>

      <ul>
      <% api_profesor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
