<%= form_for(api_curso) do |f| %>
  <% if api_curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_curso.errors.count, "error") %> prohibited this api_curso from being saved:</h2>

      <ul>
      <% api_curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
