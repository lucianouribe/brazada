<%= form_for(api_horario) do |f| %>
  <% if api_horario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_horario.errors.count, "error") %> prohibited this api_horario from being saved:</h2>

      <ul>
      <% api_horario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
